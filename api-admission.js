export async function onRequestPost({request,env}){const data=await request.json();const required=['nom','prenoms','postnom','date','tel','email','pays','ville','eglise','fonction'];if(required.some(k=>!data[k]))return new Response('Champs manquants',{status:400});if(!env.RESEND_API_KEY)return new Response('Email provider not configured',{status:503});const body=required.map(k=>`${k}: ${data[k]}`).join('\n');const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${env.RESEND_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({from:env.EMAIL_FROM||'Admissions E.M.O.E <onboarding@resend.dev>',to:['ecoleombredeleternel@gmail.com'],subject:`Nouvelle candidature E.M.O.E — ${data.nom} ${data.prenoms}`,text:body})});return new Response(await r.text(),{status:r.status,headers:{'content-type':'application/json'}})}
