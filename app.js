const modal=document.querySelector('#modal');document.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();modal.showModal()}));document.querySelector('#close').onclick=()=>modal.close();document.querySelector('#next').onclick=()=>document.querySelector('#track').scrollBy({left:360,behavior:'smooth'});document.querySelector('#prev').onclick=()=>document.querySelector('#track').scrollBy({left:-360,behavior:'smooth'});document.querySelector('#lang').onclick=()=>alert('Version anglaise en préparation.');document.querySelector('#admission').addEventListener('submit',async e=>{e.preventDefault();const status=document.querySelector('#status');status.textContent='Envoi en cours…';const data=Object.fromEntries(new FormData(e.target));try{const r=await fetch('/api/admission',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error();status.textContent='Candidature envoyée. Merci pour votre confiance.';e.target.reset()}catch{status.textContent='Le service d’envoi doit encore être configuré sur Cloudflare avec votre clé email.'}});
